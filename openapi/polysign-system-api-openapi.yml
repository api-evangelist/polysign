openapi: 3.2.0
info:
  title: Polysign System API
  license:
    name: PolySign License
    url: https://polysign.io/licenses/LICENSE-1.0
  version: '1.0'
  description: 'Operations tagged system across 3 of this provider''s published API definitions: polysign-abc-proxy-service-openapi.json, polysign-atomicnet-api-server-openapi.json, polysign-merchant-gate-openapi.json. Each path carries the servers of the definition it was published in.'
security:
- OAuth2:
  - participant
tags:
- name: System
paths:
  /v1/system/circuit_breakers:
    get:
      summary: Get the status of all circuit breakers on the server
      tags:
      - System
      responses:
        '200':
          description: Map of breakers to their statuses
          content:
            application/json:
              schema:
                type: object
      operationId: getV1SystemCircuitBreakers
      x-operation-id-source: derived
  /v1/system/ping:
    get:
      summary: Basic health check
      tags:
      - System
      responses:
        '200':
          description: System status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ping'
      operationId: getV1SystemPing
      x-operation-id-source: derived
  /v1/system/version:
    get:
      summary: Get the version of the server
      tags:
      - System
      responses:
        '200':
          description: Version
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Version'
      operationId: getV1SystemVersion
      x-operation-id-source: derived
  /v1/system/deep_ping:
    get:
      summary: Deep health check
      tags:
      - System
      responses:
        '200':
          description: Deep system status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeepPing'
      operationId: getV1SystemDeepPing
      x-operation-id-source: derived
components:
  schemas:
    DeepPing:
      type: object
      properties:
        alive:
          type: boolean
        database_reachable:
          type: boolean
      required:
      - alive
      - database_reachable
    Version:
      type: object
      properties:
        build_time:
          type: string
        version:
          type: string
    Ping:
      type: object
      properties:
        alive:
          type: boolean
    DeepPing_2:
      type: object
      properties:
        alive:
          type: boolean
        database_reachable:
          type: boolean
        schema_version:
          type: integer
          default: 1
          description: Version for the message schema.
      required:
      - alive
      - database_reachable
  securitySchemes:
    OAuth2:
      type: oauth2
      description: OAuth 2.0 with the client credentials grant flow.
      flows:
        clientCredentials:
          tokenUrl: /v1/auth/token
          scopes:
            participant: Grants access to participant.
x-refined-from:
- polysign-abc-proxy-service-openapi.json
- polysign-atomicnet-api-server-openapi.json
- polysign-merchant-gate-openapi.json
